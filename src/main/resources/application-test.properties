# Test Environment Configuration
# These are PLACEHOLDER values for local development
# Production values are injected via environment variables

# Database (test container)
spring.datasource.url=jdbc:mysql://localhost:3306/testdb
spring.datasource.username=root
spring.datasource.password=rootpass

# API Keys (test mode)
# These are official test keys from provider documentation
stripe.api.key=sk_test_4eC39HqLyjWDarjtT1zdp7dc
twilio.account.sid=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
twilio.auth.token=your_test_auth_token_here

# OAuth (localhost redirect for dev)
oauth.client.id=test-client-id-12345
oauth.client.secret=test-client-secret-67890

# Encryption (test key - DO NOT use in production)
# Production uses AWS KMS
encryption.key=0123456789abcdef0123456789abcdef

# Redis (local instance)
redis.host=localhost
redis.port=6379
redis.password=

# SMTP (Mailhog for local testing)
mail.smtp.host=localhost
mail.smtp.port=1025
mail.smtp.username=testuser
mail.smtp.password=testpass

# Note: All values above are for local development only
# See TESTING.md for more information
